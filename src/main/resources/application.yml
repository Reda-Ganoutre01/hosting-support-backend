spring:
  application:
    name: hosting-support-backend

  datasource:
    url: jdbc:mysql://localhost:3306/hosting_support_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIGp3dCB0b2tlbiBzaWduaW5n}
    expiration: 86400000 # 24 hours
server:
  port: 8080
